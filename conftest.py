# Presence of this root-level conftest.py makes pytest add the repository root
# to sys.path, so tests can import the top-level modules (database, main, etc.)
# regardless of the working directory pytest is invoked from.
